[default.extend-words]
# numpy/torch API name, not a misspelling of "arrange". Flagged inside notebooks,
# where typos parses the JSON as prose rather than as code.
arange = "arange"

ix = "ix"
iy = "iy"
iz = "iz"

# Element symbols in peng1996_element_params.json that collide with real words.
Ba = "Ba"
Nd = "Nd"

# Amino acid three-letter codes that collide with real words.
SER = "SER"
THR = "THR"

# Bonded-environment labels in elastic_scattering_bonding_*.json, e.g. "C(NOO)".
NOO = "NOO"
